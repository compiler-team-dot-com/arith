(library
 (name lib))

(menhir
 (modules grammar))

(ocamllex lexer)

(env
 (dev
  (flags
   (:standard -w -32-37)))
 ; ignore “unused stub” warnings while iterating
 (release
  (flags
   (:standard -w +32+37 -warn-error +a)))
 ; treat every warning as an error in CI/builds
 )
